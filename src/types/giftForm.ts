export enum ScentType {
  cologne = 'cologne',
  perfume = 'perfume',
}

export enum WhenToSend {
  now = 'now',
  later = 'later',
}

export enum SubscriptionPlanPrices {
  twelveMonths = 153,
  sixMonths = 89,
  threeMonths = 48,
}
